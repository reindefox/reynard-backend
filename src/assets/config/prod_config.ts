export const config = {
    /* Main config */
    clientToken: process.env.PROD_TOKEN,
    clientPrefix: "!",
    /* Server config */
    clientSecret: process.env.PROD_CLIENT_SECRET,
    /* IDs config */
    mainGuildID: "",
    client: {
        user: {
            id: ""
        }
    }
};
